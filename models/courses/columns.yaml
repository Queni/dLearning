# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Название
    category:
        label: Категория
        relation: category
        select: name